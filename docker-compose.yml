version: '3.8'

services:
  api:
    build: .
    container_name: autonomous_ds_agent
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      # Note: LLM API keys should be placed in a .env file locally
    env_file:
      - .env
